# ============================================================
# 运行态数据（**绝不能提交**）
# ============================================================
# 这些文件在源码模式下会生成在项目根目录，里面是真实库路径、
# Jellyfin 地址与 API Key、操作留痕。发布前请用
# `git add -A -n | grep -E "settings.json|archive.db|^logs/"` 自查。
settings.json
rules.json
archive.db
archive.db-journal
archive.db-wal
archive.db-shm
logs/
data/
*_state.json
backups/

# 便携模式标记（由使用者自己创建）
portable.txt

# ============================================================
# 构建产物
# ============================================================
build/
dist/
*.spec.bak
*.egg-info/
release/
*.zip
*.sha256

# 按需导出的旧版本源码快照（scripts/versions.py export）
_versions/

# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*.so
.Python
.venv/
venv/
env/
.pytest_home/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ============================================================
# 编辑器 / 系统
# ============================================================
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db
desktop.ini

# ============================================================
# 本地临时目录（历史遗留的开发脚手架）
# ============================================================
_tmp_archive/
_testdata/
_t[0-9]/
_t[A-Za-z0-9]/
*.bak_*
*.orig
*.rej
